# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
- conda-forge
- default
dependencies:
- python >=3.9
- numpy
- pandas
- matplotlib
- plotly
- biorbd
- dash
- scipy
- colorcet
- seaborn

